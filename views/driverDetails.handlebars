<h1 class="text-center">Drivers</h1>
<div class="container">
    {{!-- <a href="/create" class="btn btn-primary">+ New Movie</a> --}}
    <table class="table table-striped mt-4">
        
        <a href="addNewDriver">
            <button class="btn btn-success">Add Driver</button>

        </a>
        
        <tr>
            <th>id</th>
            <th>driver name</th>
            <th>liscense number</th>
            <th>email</th>
            <th>address</th>
            <th>dob</th>
            <th>gender</th>
            <th>password</th>


        </tr>
        
        {{#each driverDetails}}
        <tr>
            <td>{{this.dataValues.Driver_id}}</td>
            <td>{{this.dataValues.Drive_name}}</td>
            <td>{{this.dataValues.Driver_license_no}}</td>
            <td>{{this.dataValues.Driver_email}}</td>
            <td>{{this.dataValues.Driver_adress}}</td>
            <td>{{this.dataValues.Driver_dob}}</td>
            <td>{{this.dataValues.Driver_gender}}</td>
            <td>{{this.dataValues.Driver_password}}</td>

            <td>
                <a href="editdriver/{{this.dataValues.Driver_id}}">Edit</a>🚗
                <a href="deletedriver/{{this.dataValues.Driver_id}}" onclick="return confirm('Are you sure to delete this? ')">Delete</a>
            </td>

            {{!-- <td>{{data.}}</td> --}}
            {{!-- <td>{{this.mobile}}</td>
            <td>{{this.dataValues.dob}}</td> --}}
            {{!-- <td>
                <a href="/update/{{this.dataValues.id}}">Edit</a> |
                <a href="/delete/{{this.dataValues.id}}" class="text-danger"
                    onclick="return confirm('Are you sure you want to delete this')">Delete</a>
            </td> --}}
        </tr>

        {{/each}}
        
        {{#each cabDetails}}
        <tr>
            <td>{{this.dataValues.cab_name}}</td>
            <td>{{this.dataValues.cab_description}}</td>
            <td>{{this.dataValues.cab_total_capacity}}</td>
            <td>
                <a href="cabDetails/{{this.dataValues.cab_no}}">Book now</a>
            </td>

            {{!-- <td>{{data.}}</td> --}}
            {{!-- <td>{{this.mobile}}</td>
            <td>{{this.dataValues.dob}}</td> --}}
            {{!-- <td>
                <a href="/update/{{this.dataValues.id}}">Edit</a> |
                <a href="/delete/{{this.dataValues.id}}" class="text-danger"
                    onclick="return confirm('Are you sure you want to delete this')">Delete</a>
            </td> --}}
        </tr>

        {{/each}}
        

        
       

    </table>

    <p class="text-end">You are logged in as:
        <b>{{data.firstName}} {{data.lastName}}</b>
    </p>
</div>